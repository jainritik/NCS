const initialState = {
    cafes: [],
  };
  
  const cafesReducer = (state = initialState, action) => {
    switch (action.type) {
      // Define your reducer logic and handle actions for cafes state
      default:
        return state;
    }
  };
  
  export default cafesReducer;
  